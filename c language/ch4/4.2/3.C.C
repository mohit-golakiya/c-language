#include<stdio.h>
#include<conio.h>
main()
{
    clrscr();
    int a,b,c;
    printf("Enter the value of a = ");
    scanf("%d",&a);
    printf("Enter the value of b = ");
    scanf("%d",&b);
    printf("Answer : %d",(a*a*a) + (b*b*b) + (3*a*a*b) + (3*b*b*a));
    getch();




}